feat(v14.1): introduce L1 coordinate-based scientific justification engine

- Replace L1 keyword list with 4-axis L1 coordinate state (regime/slope/magnitude/structure)
- Add measurement validation gate before SJ evaluation
- Refactor SJ ontology to mechanism_proposals-based structure
- Separate FN tunneling and thermionic emission SJ into individual ontology files
- Update frontend to align with new L1 state & SJ proposal outputs
- Enforce strict SJ JSON schema consistency
